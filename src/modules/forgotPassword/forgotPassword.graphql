# import Error from "../../shared.graphql"

type Query {
  dummy4: String
}

type Mutation {
  sendForgotPasswordEmail(email: String!): Boolean!
  forgotPasswordUpdate(newPassword: String!, key: String!): [Error!]
}
